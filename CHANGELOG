## [Unreleased]

## [0.1.2] - 2021-01-28
### Added
- The trait `PartialEq` to Sppparse's structs
- The trait `SparsableTrait` to serde_json::Value
- A public function to get the `SparsePointedValue` from `SparseRefRaw`
- The `semver` feature, adding support for `semver::{Version, VersionReq}`
- A function to resolve a new pointer at runtime using a `SparseValue` or a `SparseValueMut`
- `SparseRefRawInline` to deserialize inline reference
### Changed
- How the proc macro resolve its crate name
- How the `SparseValueMut` is constructed.
- `check_depth`: now accepts `&self` as the first argument
### Removed
- Bounds on structs
- The `autoimpl` dependency

## [0.1.1] - 2021-01-20
### Added
- Support for `Option<T>`
- The `url` feature, adding support for `url::Url`

## [0.1.0] - 2021-01-18
### Added

- 🎉 first (veeeeeeeery alpha) release!
